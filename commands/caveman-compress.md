---
description: Compress a Markdown or text memory file into caveman style
---
Compress memory file: $ARGUMENTS

Use the `caveman-compress` skill. Preserve code blocks, inline code, URLs,
paths, commands, numbers, headings, list structure, and frontmatter exactly.
Back up the original as `<file>.original.md` before overwriting.
